# ~/.profile: executed by ksh(1).
#
#       $Id$
#      $URL$
#  $Created: tksnp 2007-06-27 $

# always use bash instead of ksh
exec /usr/local/bin/bash

# the following line is useful for the pause/pa alias (see .bashrc)
# eof; press Ctrl-Z to unpause
